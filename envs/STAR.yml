name: STAR
# Pinned to the version that built the existing indexes under GenomesPrefix. STAR refuses to load
# an index generated by a different major version, so bumping this means regenerating every
# STARIndex directory.
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - star=2.6.1a
